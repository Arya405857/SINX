import { useRef, useState } from "react";
import { BadgeCheck, Camera, Edit3, Mail, MapPin, Save, X } from "lucide-react";
import DashboardLayout from "../../components/dashboard/DashboardLayout";

export default function Profile() {
  const [editing, setEditing] = useState(false); const [saved, setSaved] = useState(false); const [avatar, setAvatar] = useState(""); const inputRef = useRef(null); const [profile, setProfile] = useState({ name: "Arya", email: "arya@signix.ai", location: "India", bio: "Learning sign language one conversation at a time." });
  const update = (field) => (event) => setProfile((value) => ({ ...value, [field]: event.target.value }));
  return <DashboardLayout title="Profile" activeItem="profile"><div className="mx-auto max-w-4xl space-y-6"><input ref={inputRef} type="file" accept="image/*" className="sr-only" onChange={(event) => { const file = event.target.files?.[0]; if (file) setAvatar(URL.createObjectURL(file)); }} /><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="h-28 bg-gradient-to-r from-blue-600 to-indigo-500" /><div className="px-5 pb-6 sm:px-8"><div className="-mt-12 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div className="flex items-end gap-4"><div className="relative flex h-24 w-24 items-center justify-center overflow-hidden rounded-2xl border-4 border-white bg-gradient-to-br from-blue-600 to-indigo-500 text-3xl font-semibold text-white shadow-sm">{avatar ? <img src={avatar} alt="Profile preview" className="h-full w-full object-cover" /> : "A"}<button type="button" onClick={() => inputRef.current?.click()} className="absolute -bottom-1 -right-1 rounded-full border-2 border-white bg-white p-1.5 text-slate-500 shadow-sm" aria-label="Change profile image"><Camera className="h-3.5 w-3.5" /></button></div><div><h2 className="text-xl font-semibold text-slate-900">{profile.name}</h2><p className="mt-1 flex items-center gap-1.5 text-sm text-slate-500"><BadgeCheck className="h-4 w-4 text-blue-600" /> SIGNIX member</p></div></div><div className="flex gap-2"><button type="button" onClick={() => { if (editing) setSaved(true); setEditing((value) => !value); }} className="inline-flex items-center justify-center gap-2 rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-medium text-slate-700 hover:border-blue-300 hover:text-blue-700">{editing ? <Save className="h-4 w-4" /> : <Edit3 className="h-4 w-4" />}{editing ? "Save changes" : "Edit profile"}</button>{editing && <button type="button" onClick={() => setEditing(false)} className="rounded-xl border border-slate-200 px-3 text-slate-500" aria-label="Cancel profile editing"><X className="h-4 w-4" /></button>}</div></div>{saved && <p className="mt-5 rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-700">Profile details saved locally. Connect your account service to persist changes.</p>}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><h3 className="text-lg font-semibold text-slate-900">Personal information</h3><p className="mt-1 text-sm text-slate-500">Keep your account details up to date.</p><div className="mt-6 grid gap-5 sm:grid-cols-2"><Field label="Full name" value={profile.name} disabled={!editing} onChange={update("name")} /><Field label="Email address" value={profile.email} disabled={!editing} onChange={update("email")} icon={Mail} /><Field label="Location" value={profile.location} disabled={!editing} onChange={update("location")} icon={MapPin} /><label className="sm:col-span-2"><span className="text-sm font-medium text-slate-700">About you</span><textarea value={profile.bio} disabled={!editing} onChange={update("bio")} rows="3" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 p-3 text-sm text-slate-700 disabled:cursor-default disabled:bg-slate-50" /></label></div></section></div></DashboardLayout>;
}
function Field({ label, value, disabled, onChange, icon: Icon }) { return <label><span className="text-sm font-medium text-slate-700">{label}</span><div className="relative mt-2">{Icon && <Icon className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" />}<input value={value} disabled={disabled} onChange={onChange} className={`w-full rounded-xl border border-slate-200 bg-slate-50 py-2.5 text-sm text-slate-700 disabled:cursor-default ${Icon ? "pl-9 pr-3" : "px-3"}`} /></div></label>; }

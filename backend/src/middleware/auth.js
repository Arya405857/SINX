import { User } from '../models/index.js'; import { AppError, asyncHandler } from '../utils/http.js'; import { verifyAccessToken } from '../services/tokenService.js';
export const requireAuth = asyncHandler(async (req, _res, next) => { const token = req.headers.authorization?.replace(/^Bearer\s+/i, ''); if (!token) throw new AppError('Authentication is required.', 401, 'UNAUTHENTICATED'); const claims = verifyAccessToken(token); const user = await User.findById(claims.sub); if (!user) throw new AppError('Account not found.', 401, 'UNAUTHENTICATED'); req.user = user; next(); });
